<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <!--
      ĐÃ SỬA LỖI CHO BẠN:
      1. Thêm android:id="@+id/header_layout"
      2. Đổi layout_width="0dp" (thay vì match_parent, để dùng constraint)
      3. Thêm 3 constraint (top, start, end) để neo nó vào cạnh trên.
      Nếu không có 3 constraint này, FrameLayout sẽ bay về góc 0,0.
    -->
    <FrameLayout
        android:id="@+id/header_layout"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- View nền nằm dưới -->
        <View
            android:layout_width="match_parent"
            android:layout_height="130dp"
            android:background="@drawable/bg_card" />

        <!-- Logo nằm trên -->
        <ImageView
            android:layout_width="112dp"
            android:layout_height="60dp"
            android:layout_gravity="start|center_vertical"
            android:layout_marginBottom="16dp"
            android:layout_marginStart="16dp"
            android:contentDescription="Logo"
            android:src="@drawable/logo_ntu"
            android:visibility="visible" />

    </FrameLayout>

    <!--
      ĐÃ THÊM:
      Một LinearLayout để chứa các nút, được neo BÊN DƯỚI header_layout
    -->


</androidx.constraintlayout.widget.ConstraintLayout>

